def add_last(self,e):
    newest = self._Node(e)
    newest._next = None
    self.
def delete_last(self):
    for element in self:
        if element != self._tail:
            sera_new_tail=element
        else:
            self._tail = sera_new_tail